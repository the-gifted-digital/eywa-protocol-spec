# เกต K3w เคยเอา volume ไปตัดสินการแยกหน้าที่มีคนตั้งใจแยกไว้ — แก้แล้ว เป็น DR-064

จาก smile-scape-clinic · แก้ `check-keyword-collisions.py` · **ทั้งสามแบรนด์ได้ผลทันทีโดยไม่ต้องแก้ข้อมูล**

---

## สิ่งที่เจอ

ชั้น semantic อ่าน `search_intent` + `primary_entity_fp` **สองคอลัมน์นี้อธิบายคีย์ ไม่มีอันไหนอธิบายหน้า**

```
รากฟันเทียม        commercial · Dental Implant · 3.2      procedure_pillar (T2)
รากฟันเทียม ราคา   commercial · Dental Implant · 5.13.1.7 pricing_page   (T13)
```

ตรงกันทุกคอลัมน์ที่ชั้น semantic เป็นเจ้าของ ก็เลยได้ verdict `same` แล้วตกไปที่ชั้น volume
เรานั่งดู 21 คู่ K3w ของตัวเองแล้วพบว่า **19 คู่หน้าสองฝั่งเป็นคนละ `page_category`**

**ที่อันตรายคือคู่ที่เกตตัดสินได้เอง** ไม่ใช่คู่ที่ตัดสินไม่ได้

```
ขูดหินปูน ราคา  15,350   ชนะ   ขูดหินปูน  12,325
เกตเสนอ: ปลด 'ขูดหินปูน' ลงเป็น semantic
ผลถ้าทำตาม: หน้า 3.4.1 procedure_pillar เสียชื่อตัวเองให้หน้าราคา 5.13.1.9
```

เราเกือบเดินไปทางเติม volume ให้ครบเพื่อให้เกตตัดสินได้มากขึ้น — **ซึ่งจะทำให้ข้อเสนอผิดแบบนี้
มั่นใจขึ้น ไม่ได้ทำให้ถูกขึ้น** ปัญหาไม่ได้อยู่ที่ข้อมูลไม่พอ

## แก้อะไรไป (4 อย่าง)

**1 · `pricing_split` เคลียร์คู่หน้าหลัก-หน้าราคา ก่อนชั้น semantic รัน** — แคบโดยตั้งใจ: ฝั่งเดียว
เป็น `pricing_page` · หมวดสองฝั่งต่างกัน · คีย์ที่อยู่บนหน้าราคาติด price intent
**หมวดต่างกันแบบอื่นยังส่งให้ operator หมด** เพราะ `knowledge_article` กับ `condition_pillar`
บน entity เดียวกันแย่งกันได้จริง ไม่มีคอลัมน์ไหนบอกว่าใครควรชนะ

**2 · `propose()` อ่าน volume สองคอลัมน์** — `volume_recent_12m` ก่อน เสมอเมื่อไรใช้ `volume_avg_48m`
จากแถวเดียวกัน

🔴 **อันนี้เกี่ยวกับ deezy โดยตรง** แบตช์ `2026-08-12` ของคุณมี **896 แถว** ที่ `volume_recent_12m = 0`
ทั้งที่ `volume_avg_48m` เป็นบวก **สูงสุด 90,688** ของเรา 461 จาก 525 แถวในแบตช์ `2026-07-17`
0 แบบนั้นคือค่าที่ไม่ได้กลับมา ไม่ใช่การวัดว่าไม่มีคนค้น — `รากฟันเทียม` อ่านได้ 0 recent กับ 3,450 ที่ 48 เดือน

**3 · null ไม่ใช่ 0** — ข้อความเดิม `"ไม่มี volume ทั้งสองฝั่ง"` ยิงตอนเงื่อนไขจริงเป็น
`va is None or vb is None` · คู่ที่มีเลขข้างเดียวจึงอ่านว่าไม่มีอะไรให้เทียบ ตอนนี้บอกตรง ๆ ว่าข้างไหนขาด
และ **ค่าว่างไม่ตัดสินอะไรเลย**

**4 · `--verbose` แสดงคู่ที่ถูกเคลียร์พร้อมเหตุผล** — เดิมร่องรอยเดียวคือตัวเลขนับ (ของ deezy คือ 283 คู่)
กฎที่ตรวจสอบไม่ได้คือกฎที่ไม่มีใครหาบั๊กเจอ · เราเพิ่งเติมกฎเข้าไปในกองนั้นอีกกฎ เลยต้องเปิดให้ดู

## ผลวัดสด ทั้งสามแบรนด์

```
smile-scape-clinic   K3w 21→12 · operator 19→10 · cleared 69→78
deezy-dental         K3w 23→17 · operator 16→ 6 · cleared 283→290
vth-biodent          K3w 20→17 · operator 13→12 · cleared 58→62
```

blocking ยัง 0 ทั้งสามแบรนด์ · ไม่มีคู่ไหนย้ายจาก "รายงาน" ไป "เคลียร์" โดยผิด
(บนของเรามีป้าย pricing split 10 คู่ แต่ยอดเคลียร์ +9 เพราะ `จัดฟัน | ราคาจัดฟัน` เดิมถูกชั้น semantic
เคลียร์อยู่แล้ว — เปลี่ยนป้าย ไม่ได้เปลี่ยนผล)

## กับดักที่ฝากไว้

`PRICE_INTENT_RE` ในเกต **คัดลอกมาจาก `v_seo_keyword_pool.is_price_intent`** เกตอ่านตารางดิบ
ไม่ได้อ่าน view เพราะต้องการประวัติ snapshot ที่ view ยุบเหลือแถวเดียว — **แก้ที่ไหนต้องแก้ทั้งสองที่**

`pricing_split` ต้องการ `page_category` ที่ถูกต้อง · deezy มีหน้า `pricing_page` 24 หน้าแต่มีคีย์
target ที่ติด price intent แค่ 12 — กฎจะไม่ยิงกับอีก 12 หน้านั้น ซึ่งถูกแล้ว หน้าราคาที่ target
คีย์ที่ไม่ใช่คำถามเรื่องราคาเป็นการตัดสินใจของใครบางคนที่ควรได้ดู ไม่ใช่ของที่ควรโบกผ่าน

## ที่เรายังไม่ทำ

**ไม่แตะ `target_keyword_fp`** เกตยังเสนออย่างเดียวเหมือนเดิม · คู่ที่เหลือของเรา 12 คู่
รวมถึงสองคู่ที่ชนกันจริง (หมวดเดียวกันทั้งคู่) ยังรอ operator ตัดสิน

**ไม่ไปเติม volume** — เราตั้งใจจะทำ แล้วพบว่ามันไม่ได้ปิด K3w เลย 19 จาก 21 คู่ไม่ใช่ปัญหา volume
ตั้งแต่แรก · volume ที่ว่างจะสำคัญตอนจัดลำดับว่าจะเขียนหน้าไหนก่อน ซึ่งเป็นคนละงาน

**หมายเหตุเรื่องความมั่นใจ:** รอบก่อนเราส่งข้อสรุปผิดเรื่อง `semantic_verdict` case-sensitivity
ไปให้ vth ด้วยความมั่นใจ (เราถือหลักฐานที่ค้านตัวเองอยู่ในมือแต่ไม่ได้เอามาทดสอบข้อสรุป)
รอบนี้จึงวัดทั้งสามแบรนด์ก่อน-หลังก่อนส่ง ตัวเลขข้างบนรีโปรดิวซ์ได้ด้วย
`git show HEAD:scripts/citation-gates/check-keyword-collisions.py` แล้วรันเทียบ

---

**Spec:** DR-064 · `scripts/citation-gates/check-keyword-collisions.py` · MANIFEST ปั่นใหม่แล้วครบ 14 ไฟล์
