# Authority Scoring SOP — v1.0 (universal)

> ขอบเขต: การให้คะแนน authority ทั้ง 3 ชั้น — citation, entity, cluster — สำหรับทุกแบรนด์ที่ใช้ EYWA Protocol
> Schema: `seo_citations.citation_authority_weight` · `seo_entity_graph.entity_authority_score` · `seo_topic_cluster_master.cluster_topical_authority` + `cluster_health_score`
> คู่กับ: [Citation_Pool_SOP_v1_0.md](Citation_Pool_SOP_v1_0.md) — สระต้องสะอาดก่อน ถึงจะให้คะแนนมีความหมาย
>
> **เกิดจากอะไร:** สคีมามีคอลัมน์คะแนนมาตั้งแต่สร้างสระ พร้อม `authority_formula_version` ประทับว่า `bible-23.1`
> แต่ไม่เคยมีค่าเลยสักแถว — citation 0/390, entity 0/715, cluster 0/58
> ตามหาสูตร Bible 23.1 ทั้งรีโปแล้วไม่มี และ operator ก็ไม่มี
> 2026-08-09 จึงตัดสินใจ**นิยามสูตรขึ้นเอง** แทนที่จะปล่อยคอลัมน์ตายหรือกรอกมือ (ดู DR-VTH-011)

---

## 1. หลักการ

> **คะแนนที่แกะไม่ได้ แย่กว่าไม่มีคะแนน**

กฎ 4 ข้อที่ทำให้สูตรนี้เถียงได้ ไม่ใช่ตัวเลขที่ใครคนหนึ่งชอบ:

1. **ทุกองค์ประกอบอ่านจากข้อมูลที่มีหรือดึงได้จาก API ฟรี** ไม่มีขั้นไหนเป็นดุลพินิจ รันซ้ำได้ผลเดิมเป๊ะ
2. **เก็บ breakdown ไว้ข้างคะแนนเสมอ** ทุกตัวเลขต้องถอดออกมาดูได้ว่ามาจากไหน
3. **`authority_formula_version` ต้องเป็นชื่อสูตรของตัวเอง** ปัจจุบันคือ `eywa-authority-1.0` — ห้ามประทับ `bible-23.1` เพราะนี่ไม่ใช่สูตรนั้น
4. **รันซ้ำได้ไม่มีผลข้างเคียง** แก้น้ำหนัก → bump version → รันใหม่ทับ

**สเกล 0–10 ทั้งสามชั้น** เพราะ `citation_authority_weight` เป็น `numeric(4,3)` เพดาน 9.999 สคีมากำหนดมาแล้ว และการใช้สเกลเดียวกันทำให้เทียบข้ามชั้นได้ คะแนนเต็มเก็บเป็น 9.999

---

## 2. ชั้นที่ 1 — citation

`compute-citation-authority.py`

| องค์ประกอบ | เต็ม | มาจาก | ทำไม |
|---|---|---|---|
| tier base | 5.5 | `citation_tier` (Bible 23.1) | ลำดับชั้นหลักฐานคือแกนหลัก ให้มันหนักที่สุด |
| recency | 2.0 | อายุเทียบเพดานความสดของ tier | **ยกเพดานมาจาก gate G8 ตรง ๆ** สูตรกับ gate จะได้ขัดกันไม่ได้ |
| corroboration | 1.5 | OpenAlex FWCI | สัญญาณเดียวที่มาจากนอกระบบเรา |
| source authority | 1.0 | `source_org_fp` → ประเภทและ tier ขององค์กร | guideline ของ WHO กับของชมรมเล็ก ๆ ไม่เท่ากัน |

**tier base:** T1 5.5 · T2 4.7 · T3 4.2 · T4 3.8 · T5 2.6 · T6 1.5
ช่องว่างกว้างที่สุดคือ T4→T5 ซึ่งเป็นเส้นแบ่งระหว่าง guideline กับงานสังเกต

**recency:** เต็มถ้าอายุ ≤ เพดาน แล้วลดเป็นเส้นตรงจนถึง 0 ที่ 2 เท่าของเพดาน
เพดาน (ปี): T1 5 · T2 7 · T5 7 (สามตัวนี้ตรงกับ G8) · T3/T4 10 (guideline ถูกแทนที่ราวทศวรรษ) · T6 15

**corroboration:** FWCI normalize ตามสาขาและปีแล้ว 1.0 = ค่าเฉลี่ยโลก
≥3.0 → 1.5 · ≥2.0 → 1.2 · ≥1.5 → 1.0 · ≥1.0 → 0.7 · ≥0.5 → 0.4 · >0 → 0.2
ถ้าไม่มี FWCI ใช้จำนวนอ้างอิงดิบแบบ log แต่**ตัดเพดานที่ 0.8** — ค่าดิบให้รางวัลกับความแก่ไม่ใช่อิทธิพล ของเดาห้ามชนะของวัด

**source authority:** จาก `source_org_fp` → `seo_entity_organization.organization_type` (+ `authority_tier`)

| organization_type | tier | คะแนน |
|---|---|---|
| `government_agency` | `tier_1_regulatory` | 1.0 |
| `regulator` | ใดก็ได้ | **1.0** |
| `government_agency` | — | 0.9 |
| `professional_association` | — | 0.8 |
| `accreditation_body` | — | 0.7 |
| `university` / `research_institute` | — | 0.6 |
| `hospital` / `clinic` | — | 0.3 |
| `manufacturer` | — | 0.1 |
| ไม่มีองค์กรผูกอยู่ | — | 0.0 |

🔴 **ชนิดที่ไม่อยู่ในตารางนี้คืนค่า `None` ไม่ใช่ `0.0`** และ `--apply` **ปฏิเสธการเขียน**จนกว่าจะเติมให้ครบ
ก่อน **DR-060** (2026-08-24) ชนิดที่ไม่ได้แมปตกไป `0.0` เงียบ ๆ ซึ่งเป็น**ค่าเดียวกับ "ไม่มีองค์กรหนุนเลย"**
`regulator` เป็นหนึ่งในนั้น ทั้งที่เป็นชนิดองค์กรที่ช่อง 1.0 เขียนขึ้นมาเพื่อมันโดยตรง — ใบของทันตแพทยสภา
เก็บ 5.8 ขณะที่ฝาแฝดชนิด `government_agency` เก็บ 6.8 ทั้งที่เหมือนกันทุกแกนอื่น (แก้แล้ว เป็น 6.8 ทั้งคู่)
**คะแนนอำนาจเป็นนโยบาย ไม่ใช่ค่า default ที่สคริปต์ควรเดา**

🔴 **`--apply` ปฏิเสธการเขียนถ้า OpenAlex ตอบไม่ครบด้วย** — งานที่เรียกไม่ติดจะได้ corroboration `0.0`
ซึ่งหน้าตาเหมือน "ไม่มีสัญญาณภายนอกจริง ๆ" แล้วทับคะแนนเดิมด้วย version stamp เดิม ตรวจย้อนไม่ได้เลย

**บทลงโทษ:** ถูกถอน/ลิงก์เสีย → 0 · ยังไม่ verify → เพดาน 2.0

---

## 3. ชั้นที่ 2 — entity

`compute-entity-cluster-authority.py`

entity แข็งแค่ไหน = ยึดโยงกับโลกภายนอกได้แค่ไหน + หลักฐานที่หนุนหน้าที่ถือมันหนักแค่ไหน

| องค์ประกอบ | เต็ม | มาจาก |
|---|---|---|
| external grounding | 3.0 | `wikidata_id` 1.5 + `icd_10_code` 1.5 |
| evidence backing | 4.0 | ค่าเฉลี่ย `citation_authority_weight` ของ citation บนหน้าที่ถือ entity นี้เป็น primary |
| corpus presence | 2.0 | จำนวนหน้าที่ถือ (primary หรือ related) สเกล log อิ่มที่ ~20 หน้า |
| lifecycle | 1.0 | mature 1.0 · growing 0.6 · อื่น 0.3 |

`evidence_backing` เชื่อมชั้น 1 เข้ากับชั้น 2 โดยตรง — สระที่สะอาดขึ้นดันคะแนน entity ขึ้นเอง ไม่ต้องแก้สองที่

---

## 4. ชั้นที่ 3 — cluster สองตัว แยกกันโดยเจตนา

**`cluster_topical_authority`** = ค่าเฉลี่ย `entity_authority_score` ของ entity ในคลัสเตอร์ **ถ่วงน้ำหนักด้วยจำนวนหน้า**
entity ที่มี 12 หน้าถือ บอกอะไรเกี่ยวกับคลัสเตอร์มากกว่า entity ที่มีหน้าเดียว

**`cluster_health_score`** = สถานะการทำงาน คนละเรื่องกับความแข็งของหัวข้อ

| องค์ประกอบ | เต็ม | มาจาก |
|---|---|---|
| citation coverage | 4.0 | สัดส่วนหน้าที่มีเนื้อหาซึ่งผ่าน `minimum_per_layer` |
| keyword coverage | 2.0 | สัดส่วนหน้าที่มี target keyword |
| entity grounding | 2.0 | สัดส่วน entity ที่มีตัวระบุภายนอก |
| publication progress | 2.0 | สัดส่วนหน้าที่ `status='Live'` |

**ทำไมต้องแยกสองตัว:** คลัสเตอร์ **แข็งแต่ไม่สุขภาพดีได้** — entity ดี หลักฐานครบ แต่ยังไม่ปล่อยอะไรเลย
ถ้ารวมเป็นตัวเดียวสภาพนี้จะหายไปในค่าเฉลี่ย ทั้งที่มันคือสิ่งที่ควรเห็นที่สุดตอนวางแผน wave

---

## 5. ลำดับการรัน

ต้องเรียงตามนี้ เพราะชั้นบนกินผลจากชั้นล่าง

```bash
python3 content-plan/etl/run-citation-qa-gates.py --brand <slug>   # ต้อง 0 blocking ก่อน
python3 content-plan/etl/compute-citation-authority.py --apply
python3 content-plan/etl/compute-entity-cluster-authority.py --apply
```

รันใหม่เมื่อ: เพิ่ม/แก้ citation · ผูก citation เข้าหน้าใหม่ · เพิ่ม entity หรือหน้า · ก่อนวางแผน wave การเขียน

---

## 6. บทเรียน

| # | กับดัก | เกิดอะไร | กันยังไง |
|---|---|---|---|
| A1 | **คอลัมน์เขียนได้ ไม่ได้แปลว่าควรเขียน** | ทดสอบแล้วคอลัมน์คะแนนไม่ใช่ generated column กรอกมือได้หมด — ซึ่งคือเหตุผลที่ต้องมีกฎห้าม ไม่ใช่เหตุผลที่จะกรอก | ถ้าคอลัมน์คู่กับ `*_formula_version` แปลว่ามันเป็นผลลัพธ์ของสูตร ไม่ใช่ช่องกรอก |
| A2 | **สเกลอยู่ในสคีมาแล้ว** | ออกแบบสูตรเป็น 0–100 เขียนไม่ลงเพราะ `numeric(4,3)` overflow ที่ 90.0 — เพดาน 9.999 บอกอยู่แล้วว่าสเกลคือ 0–10 | อ่านความละเอียดของคอลัมน์ก่อนออกแบบสูตร |
| A3 | **เพดานความสดต้องมาจาก gate** | ถ้าตั้งเพดาน recency เองแยกจาก G8 จะได้ระบบที่ gate บอกว่าเก่าแต่คะแนนบอกว่าสด | import ค่าจาก gate ตรง ๆ อย่า copy ตัวเลข |
| A4 | **ตรวจสูตรด้วยสิ่งที่รู้คำตอบอยู่แล้ว** | `bruxism-clenching` ออกมา health สูงสุด 8.00 ซึ่งเป็นคลัสเตอร์เดียวที่เขียนจบและ deploy จริง — เป็นสัญญาณว่าสูตรอ่านสถานะจริงได้ | ก่อน `--apply` ดู top/bottom แล้วถามว่าตรงกับที่รู้ไหม ถ้าไม่ตรงคือสูตรผิด ไม่ใช่ข้อมูลผิด |
| A5 | **ไม่มีอะไรได้ 10 เต็ม และนั่นถูกแล้ว** | citation สูงสุด 9.0 · entity สูงสุด 7.94 เพราะ evidence_backing เต็มต้องมี citation เฉลี่ย 10.0 ซึ่งไม่มีทางเกิด | อย่าดัดสูตรให้มีคนได้เต็ม สเกลที่ซื่อสัตย์สำคัญกว่าสเกลที่สวย |

---

## 7. Change control

- แก้สูตร = bump `FORMULA_VERSION` ในสคริปต์ + bump version ไฟล์นี้ + บันทึกเหตุผลใน changelog + รันใหม่ทั้งสามชั้น
- ห้ามแก้น้ำหนักโดยไม่ bump version — คะแนนที่คำนวณคนละสูตรแต่ประทับเวอร์ชันเดียวกันเทียบกันไม่ได้
- ถ้า Bible 23.1 ฉบับจริงโผล่มาทีหลัง: เทียบกับสูตรนี้ แล้วตัดสินว่าจะแทนหรือจะเก็บทั้งคู่ — อย่าเงียบ ๆ เปลี่ยนค่า `authority_formula_version` ให้เป็น `bible-23.1` โดยไม่คำนวณใหม่

### Changelog
- **v1.0** (2026-08-09) — ตั้งต้น `eywa-authority-1.0` หลังพบว่าสูตร Bible 23.1 ไม่มีอยู่จริงในรีโปและ operator ไม่มี · ให้คะแนน citation 397 · entity 731 · cluster 58 ครบทุกแถวเป็นครั้งแรก
