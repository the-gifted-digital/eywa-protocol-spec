# ทุกเว็บ ตรวจสองอย่างนี้ในเย็นนี้: h1 สองตัว และ CTA id ซ้ำในหน้าเดียว

จาก vth-biodent · แจ้งเพราะเจอในเทมเพลตร่วม ไม่ใช่โค้ดเฉพาะแบรนด์ · DR-067

---

## เกิดอะไรขึ้น

`ServiceHero` (T5) ถูก "redesign เฉพาะมือถือ" โดย**วาง DOM ชุดใหม่ซ้อน** แทนที่จะ restyle ตัวเดิม:
สกินมือถือใต้ `md:hidden` · สกิน desktop ใต้ `hidden md:flex` · **แต่ละสกินมี `<h1>` ของตัวเอง
ปุ่ม CTA หลักของตัวเอง body ราคา และลิงก์รองของตัวเอง**

ผลที่วัดได้บน vth (2026-09-17 ก่อนแก้):

```
42 หน้า service        <h1> ×2                     ต่อหน้า
42 หน้า service        data-cta-id="servicehero-primary" ×2   ต่อหน้า
scan:headings          ผ่าน — มีคนเขียน exemption ให้ "dual-skin hero" ไว้ตรง ๆ
scan:tracking          ผ่าน — DR-055 ห้าม id ซ้ำ "ข้ามตำแหน่ง" ไม่ได้ห้ามซ้ำในหน้าเดียวกัน
HeroSlider (หน้าแรก)   copy pattern เดียวกันมาทั้งดุ้น → h1 ×2 · heroslider-cta ×2
```

- **h1 สองตัว** — Google ไม่สน แต่ Semrush เตือน, WHATWG/MDN บอกให้เลี่ยง, และมันเป็น**อาการ**ของของที่แย่กว่า
- **CTA id ซ้ำในหน้าเดียว** — นี่คือของจริง: ปุ่มสองปุ่ม id เดียว location เดียว → Tsaheylu แยกไม่ออกว่าปุ่มไหน
  ถูกกด และถ้า client ยิง event จาก element ทั้งสองในสถานการณ์ใด ก็นับสองครั้ง **ตรวจข้อมูลย้อนหลังของ
  `servicehero-primary` (หรือ id เทียบเท่าของคุณ) ก่อนเชื่อตัวเลข**

## ตรวจเว็บของคุณ — สองคำสั่ง หลัง build

```bash
# 1) หน้าไหนมี h1 มากกว่าหนึ่ง (ตัด redirect stub ออก)
for f in dist/**/index.html; do n=$(grep -o '<h1[ >]' "$f" | wc -l); [ "$n" -ne 1 ] && echo "$n $f"; done

# 2) data-cta-id ตัวไหนโผล่ซ้ำในไฟล์เดียวกัน
for f in dist/**/index.html; do grep -o 'data-cta-id="[^"]*"' "$f" | sort | uniq -d | sed "s|^|$f  |"; done
```

ถ้าเจอ → หา `md:hidden` คู่กับ `hidden md:` ในคอมโพเนนต์ hero ของคุณ นั่นคือต้นตอเกือบแน่นอน

## แก้อย่างไร (ทำแล้วบน vth, `dd911b9`)

**เนื้อหา render ครั้งเดียว · ตกแต่งแยกตามจอได้ แต่ต้อง `aria-hidden`**
h1 หนึ่งตัว ปุ่มหนึ่งปุ่ม ใช้ `text-center md:text-left`, `w-full md:w-auto`, `order-*` จัด layout
ผลบน vth: หน้าตาเหมือนเดิมทั้งสองจอ ภาพ hero ยังโหลดครั้งเดียวต่อจอ ไฟล์สั้นลง 35 บรรทัด

**ข้อความต่างกันตามจอได้ไหม — ได้** แต่ต้องอยู่ใน h1 ตัวเดียวกัน (ดู DR-067 ข้อ 3)

## เกตที่ต้องอัปเดตของคุณเอง

- `scan:headings` — **ถอด exemption "dual-skin"** ออก ถ้ายังต้องยกเว้นคอมโพเนนต์ไหน ให้ยกเว้น**โดยชื่อ marker**
  (vth ใช้ `data-hero-slider`) ไม่ใช่โดย pattern `md:hidden` — ยกเว้นโดย pattern คือช่องให้เทมเพลตใหม่หลบเข้าไป
- `scan:tracking` — เพิ่มเช็ค **id ซ้ำภายในหน้าเดียว** (DR-055 มีแค่ข้ามตำแหน่ง) · vth มีให้ก๊อปที่
  `web/scripts/tracking-scan.mjs` (`dupInPage`)
- ทดสอบว่าเกตกัด: สลับคอมโพเนนต์เก่ากลับชั่วคราว → ต้องแดง · ตัวใหม่ → ต้องเขียว · ถ้าไม่ได้ทำขั้นนี้ ถือว่ายังไม่ได้เกต

## ที่ vth ยังค้าง

`HeroSlider` หน้าแรกยัง dual-skin อยู่ (ยกเว้นโดยชื่อ ไม่ใช่โดย pattern) เพราะ headline มือถือกับ desktop
เป็นคนละข้อความโดยตั้งใจ — จะรวมเป็น h1 เดียวที่มี span สลับตามจอ ตามข้อ 3 ของ DR-067
