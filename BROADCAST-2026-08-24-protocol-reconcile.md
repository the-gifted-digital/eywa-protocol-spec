# BROADCAST 2026-08-24 — เอกสารกลางถูกกวาดใหม่ทั้งชุด · เกตย้ายมาอยู่ที่นี่ · สิ่งที่แต่ละแบรนด์ต้องทำ

> **ถึง:** ทุกแบรนด์ที่ใช้ EYWA Protocol · **จาก:** งาน reconciliation 2026-08-23/24
> **สถานะเอกสาร:** merge เข้า `main` แล้ว — clone ใหม่ได้ของถูกทันที
> **อ้างอิง:** DR-057 (เก้าคำตัดสิน) · DR-058 (ข้อห้ามคลินิก) · รายงานเต็มที่
> `eywa-vth-biodent/content-plan/protocol-db-reconcile-2026-08-23.md`

---

## 1. ทำไมถึงต้องมี broadcast นี้

ตรวจเอกสารกลางทั้งชุดเทียบฐานข้อมูลจริง — **2,317 ข้อความที่ตรวจได้ · ไม่ตรง 1,837 · และ 336 ข้อเป็นกฎที่ยิงไม่ออกเลย**

กฎที่ยิงไม่ออกคือปัญหาที่หนักที่สุด เพราะมันคืน "0 violations" ทุกครั้ง แล้วคนอ่านว่า "ผ่าน" ทั้งที่ความจริงคือ "ไม่เคยตรวจ"

ตัวอย่างที่ชัดที่สุด: `trg_validate_medical_signoff` ติดตั้งอยู่จริงและ armed แต่ดักที่ `edge_type='contraindicates'` ซึ่ง **มี 0 แถวจาก 1,089** ข้อห้ามทางคลินิกจริง 32 ข้อถูกเก็บเป็น `related_to` — trigger มองไม่เห็นเลยตลอดอายุการใช้งาน

---

## 2. สิ่งที่เปลี่ยน และคุณต้องทำอะไร

### 2.1 เกตย้ายมาอยู่ที่ `eywa-protocol-spec/scripts/citation-gates/` 🔴 ต้องทำ

เดิมสคริปต์เกต 9 ตัวอยู่ใน repo ของ VTH ทั้งที่รับ `--brand` ทุกตัว — แบรนด์อื่นจึงเรียกไม่ได้

ต่อเข้า `web/package.json` ของคุณ (deezy กับ smile-scape ต่อให้แล้ว):

```json
"gates:verify":            "cd ../../../eywa-protocol-spec/scripts/citation-gates && shasum -a 256 -c MANIFEST.sha256",
"check:citations":         "python3 ../../../eywa-protocol-spec/scripts/citation-gates/run-citation-qa-gates.py --brand <brand_id>",
"check:citation-usage":    "python3 ../../../eywa-protocol-spec/scripts/citation-gates/verify-page-citation-usage.py --brand <brand_id>",
"check:content-citations": "python3 ../../../eywa-protocol-spec/scripts/citation-gates/audit-content-locators.py --root src/content --brand <brand_id>",
"gen:page-taxonomy":       "python3 ../../../eywa-protocol-spec/scripts/citation-gates/derive-page-role-category.py --brand <brand_id>"
```

ใส่คีย์ที่ `.secrets/supabase.env` (ดู `.secrets/README.md`) — **อย่า commit คีย์**

**`MANIFEST.sha256` คือของสำคัญ** — vendoring ไม่ผิด แต่สำเนาที่แตกแล้วเงียบเป็นเดือนคือสิ่งที่พังจริง เกิดมาแล้ว 4 ครั้ง (Keyword SOP อยู่ 4 ที่ 3 รุ่น · Citation Pool SOP อยู่ 2 ที่ ต่างกัน 2 รุ่น ทั้งคู่เขียนว่า "universal") ตรวจก่อนเชื่อผลเสมอ

### 2.2 เอกสารที่ย้ายมารวมที่ spec repo

- `Citation_Pool_SOP_v1_0.md` — **v1.5** เป็นฉบับเดียวที่ใช้ได้ · สำเนาใน VTH ถูกแทนด้วยตัวชี้
- `Authority_Scoring_SOP_v1_0.md` — เดิมมีอยู่แบรนด์เดียว เป็นสูตรที่ Bible ไม่มี แปลว่าอีกสองแบรนด์ให้คะแนน authority โดยไม่มีสูตรเขียนไว้

**อย่าสร้างสำเนาใหม่ในแบรนด์อีก** เส้นแบ่งอยู่ใน DR-057

### 2.3 คำศัพท์ที่เปลี่ยน 🔴 กระทบ ETL ของคุณ

| คอลัมน์ | เดิม | ตอนนี้ |
|---|---|---|
| `funnel_stage` | top / mid / bottom | **awareness / consideration / decision / retention** |
| `schema_markup_type` | บางแบรนด์เป็น `{A,B}` | **scalar ล้วน** |
| `sitemap_section` | deezy เป็น slug | **เลข `'1'`–`'9'` ทุกแบรนด์** |
| `intent_source_tier` | paa / derived / template_only | **+ `brand`** (ใช้จริง 88 แถว) · `paa` ไม่เคยถูกเขียนเลย และคอลัมน์ที่มันอ้างไม่มีจริง |

ถ้า ETL คุณยังเขียนค่าเก่า จะไม่ error แต่จะหลุดจากกฎที่ key ด้วยค่าใหม่

### 2.4 `layer` ไม่มีและจะไม่มี

กฎ ~229 ข้อเคย key ด้วยคอลัมน์นี้ เขียนใหม่หมดแล้วเป็น `coalesce(page_category, page_type)` ตารางแปลงอยู่ที่ **Bible §3.2**

- L4 Concern → `condition_pillar` เป็นตัวเดียวที่ map สะอาด
- L1/L2/L3/L5 เป็น approximate ระบุ caveat ไว้ในตาราง
- **L6 Protocol ไม่มีอะไรแทนได้** มาร์ก UNIMPLEMENTABLE ไว้ ไม่ได้ลืม

🔴 **ห้าม drop `page_type`** — predicate ใหม่ยังต้องใช้มันเป็น fallback

### 2.5 `content_format` เป็นของแบรนด์

DR-057: แต่ละแบรนด์ออกแบบเทมเพลตเอง โค้ดต่างกันได้ (deezy ใช้ `T2b`/`T6a`/`T8g` ที่คนอื่นใช้ `T2`/`T6`)
**สิ่งที่ห้ามคือโค้ดที่ไม่มีนิยาม** — ต้องชี้ได้ว่าโค้ดนั้นอยู่ใน content template ของแบรนด์ตรงไหน
`derive-page-role-category.py` จึงถามข้อมูลของแบรนด์เอง ไม่มีตาราง T-code กลางอีกแล้ว

### 2.6 `has_medical_review` = หน้าที่ operator อนุมัติให้ Live

ไม่ต้อง gate ด้วยแถวในตาราง review · Live แปลว่ารีวิวแล้ว ฝังลง schema ได้เลย

---

## 3. สภาพของแต่ละแบรนด์ ณ 2026-08-24

| | deezy-dental | vth-biodent | smile-scape-clinic |
|---|---|---|---|
| `sitemap_section` เลข | 869/869 | 761/761 | 728/728 |
| `funnel_stage` คำศัพท์ใหม่ | 864/869 | ✅ | ✅ |
| `schema_markup_type` scalar | 868/869 | ✅ | ✅ |
| `page_category` | 776/842 | **686/686** | 707/728 |
| `page_language` | ✅ | ✅ | ✅ |
| **citation blocking** | **15** | **0** | **0** |

---

## 4. งานที่ค้างของแต่ละแบรนด์

### deezy-dental 🔴 อ่านข้อนี้ก่อน

`check:citations` บอกว่าเหลือ 15 แถว **แต่นั่นไม่ใช่ระยะห่างจากเขียวจริง** รัน `check:citation-usage` แล้วได้:

```
cited by the page: 198 · bound but never cited: 941 · cited but not in the pool: 368
```

- **941 binding ที่หน้าไม่เคยอ้างจริง** — `seo_page_citations` บอกว่าหน้ายืนอยู่บนหลักฐานชิ้นนั้น แต่ YAML ของหน้าไม่ได้อ้างเลย
- **368 รายการอ้างอิงในเนื้อหาที่ไม่มีแถวในสระ** — อ้างอยู่บนหน้าจริงแต่ไม่มีใครตรวจว่า DOI นั้นมีอยู่ไหม

🔴 **ห้ามแก้ 15 แถวด้วยการผูก citation เพิ่ม** — G6 นับจำนวน binding ดังนั้นการผูกอะไรก็ได้จะทำให้ตัวเลขเขียว โดยที่หน้าไม่ได้อ้างจริง นั่นคือการเลี้ยงเกต และเป็นสาเหตุที่ 941 แถวนั้นเกิดขึ้นตั้งแต่แรก

ลำดับที่ถูก:
1. รัน `check:citation-usage` แล้วปลด binding ที่หน้าไม่ได้อ้าง (`status='removed'`)
2. รัน `check:content-citations` — จะ round-trip DOI ทุกใบในเนื้อหาเทียบ Crossref/PubMed · 368 รายการที่ไม่อยู่ในสระต้องเข้าสระหรือถูกถอด
3. **ค่อย**ดูว่าหน้าไหนยังต่ำกว่าขั้นต่ำจริง แล้วหา citation ที่ตรงเรื่อง เขียนลงเนื้อหา แล้วผูก

**หน้าที่ไม่ใช่เนื้อหาคลินิก ใช้ CITATION EXEMPTION ได้** — `social-security-dental-faq` (ระเบียบประกันสังคม) · `aaci-standards-explained` (มาตรฐานการรับรอง) · `smile-job-interview` เขียน `CITATION EXEMPTION` ต่อท้าย `reconciliation_notes` ของหน้านั้น (คอลัมน์นี้เป็น log ต่อท้ายด้วย `" | "` **ห้ามเขียนทับ**)

**อีก 66 แถวที่หมวดยังไม่ลง** — `gen:page-taxonomy` พิมพ์ให้แล้วว่าแถวไหน 38 แถวกำกวม (พี่น้อง `content_format` เดียวกันบอกไม่ตรงกัน) · 28 แถวไม่มีสัญญาณเลย (CSR news, implant-check tool, popup) สคริปต์ไม่เดาให้ ต้องมีคนตัดสิน

### smile-scape-clinic

- **21 แถวหมวดกำกวม** — `gen:page-taxonomy` พิมพ์รายชื่อให้แล้ว
- **`authority_weight` 0/728** — DR-021 ระบุว่าเป็น **manual allocation 1–100** ไล่ตาม `node_tier` (A 80–100 · B 60–80 · C 40–60 · D 25–45) เป็นงานบรรณาธิการ ไม่ใช่บั๊ก และไม่มีสคริปต์ไหนคำนวณให้
- **8 citation ที่ไม่มี locator** — เป็นชื่อเอกสารที่ยังต้องไปตามหา (EFP · EAO · ADA/AAO · ILAPEO) ตอนนี้อยู่ใน `G1w` ซึ่งเป็น warn ไม่บล็อกแล้ว เพราะยังไม่ผูกกับหน้าไหนและมาร์ก `unverified` ไว้ตรงไปตรงมา

### vth-biodent

blocking 0 · เหลือ 5 เส้นข้อห้ามคลินิกที่หา citation ไม่ได้ และ 15 เส้นที่รอ sign-off ทางคลินิก

---

## 5. กติกาที่ทั้งหมดนี้พยายามบังคับ

> **เกตที่คืนศูนย์เพราะตรวจแล้วไม่เจอ กับเกตที่คืนศูนย์เพราะตรวจไม่ได้ ต้องไม่หน้าตาเหมือนกัน**

finding หลายตัวมีอยู่เพื่อการนี้โดยเฉพาะ — `G1w` สำหรับ citation ที่ยังไม่มีใครใช้ · `G6u` สำหรับหน้าที่ระบุ section ไม่ได้ · `G14u` สำหรับ `study_type` ที่ไม่มี map ไหนรู้จัก

ก่อนมี `G14u` มี **174 จาก 551 แถวในสระที่ดูเหมือนถูก cross-check แล้ว เพราะช่องมีค่า ทั้งที่ไม่มีอะไรเทียบมันเลย**

กับดักเดียวกันเจอในโค้ดสองครั้งในสัปดาห์นี้:

- `warn = "w_" in gate` — คำว่า **belo(w_)minimum** ทำให้เกตที่ต้องบล็อกหน้า Live ที่หลักฐานไม่ถึงขั้นต่ำ ขึ้น WARN มาตลอดตั้งแต่วันที่ตั้งชื่อ
- ตัวจัดประเภทเดียวกันนั้นเทียบ gid ทั้งก้อน ทำให้ `G14u` ไม่ตรงกับ `G14`

---

## 6. เรื่องหลักฐานทางคลินิก (DR-058) — อ่านถ้าคุณเขียนหน้า YMYL

ค้น citation ให้ข้อห้ามทางคลินิก 32 เส้น แล้ว verify แบบ adversarial ทุกใบ **PMID ทุกใบจริง ตรวจซ้ำด้วยมือ**

🔴 **ไม่มีสักใบที่รองรับข้อห้ามเด็ดขาด** และหกใบพูดตรงข้ามกับที่ฐานข้อมูลอ้าง:

| แหล่ง | พูดว่า |
|---|---|
| International ONJ Taskforce 2025 | ไม่ต้องหยุดยาต้านการสลายกระดูกก่อนฝังรากเทียม · ฝังได้อย่างปลอดภัย |
| SR เบาหวาน 2022 | "ภายใต้ภาวะที่ควบคุมได้ **ยังไม่มีข้อห้าม**" |
| MA ถอนฟันในผู้ป่วยมะเร็ง 2026 | การถอนฟันไม่ใช่ตัวกระตุ้นที่ต้องเลี่ยงเสมอไป |
| SR ผู้ปลูกถ่ายอวัยวะ 2021 | รากเทียมรอด 100% ที่ 60 เดือน — บทความยกเองว่า "เคยถูกถือเป็นข้อห้ามมาหลายปี" แล้วปฏิเสธ |

**กับดักที่ต้องกันซ้ำ:**

- **อวัยวะถูก โรคถูก หัตถการผิด** — งาน MRONJ ของการถอนฟันไม่ครอบ sinus lift · bisphosphonate ชนิดฉีดไม่ครอบชนิดกิน · เบาหวานที่ควบคุมได้ไม่ครอบที่ควบคุมไม่ได้
- **ชื่อเรื่องไม่ได้กำหนด tier** — เจอบทความที่เรียกตัวเองว่า systematic review แต่ PubMed จัดเป็น Journal Article ธรรมดา (8 case report) เจอ 2 ครั้ง
- **ตัวเลขคนละกลุ่ม** — MRONJ หลังถอนฟัน: มะเร็ง 3.2% เทียบกระดูกพรุนชนิดกิน **0.15%** ต่างกัน 20 เท่า หยิบผิดกลุ่มคือการขู่ผู้อ่านด้วยความเสี่ยงของคนอื่น
- 🔴 **ห้ามเติม `medical_reviewer_signoff_at` โดยไม่มีคนเซ็นจริง ไม่ว่ากรณีใด**

---

## 7. ยังพิสูจน์ไม่ได้ — อย่าเชื่อว่าถูกเพียงเพราะเอกสารเขียนไว้

PostgREST อ่าน `pg_constraint` / `pg_indexes` / RLS policy ไม่ได้ ดังนั้นคำอธิบาย **body ของ trigger และ CHECK · รายการ index · RLS policy · โครง partition ของ `logs_YYYY`** ยังค้างอยู่ที่สภาพ **2026-05-30** เอกสารเขียนคาเวียตไว้แล้ว แต่ไม่ได้แปลว่าถูก

ใครมีสิทธิ์เข้า psql โดยตรง ช่วยตรวจชั้นนี้แล้วส่งกลับมาจะช่วยได้มาก
